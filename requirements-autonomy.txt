# S3M Phase 6 — Autonomy & Swarm Dependencies
# Install with: pip install -r requirements-autonomy.txt

# Reinforcement Learning (optional — system falls back to builtin if not installed)
# ray[rllib]>=2.9.0
# stable-baselines3>=2.2.0
# gymnasium>=0.29.0
# pettingzoo>=1.24.0

# Behavior Trees (optional — builtin BT engine used if not installed)
# py-trees>=2.2.0

# XAI (optional — context-based explanations used if not installed)
# captum>=0.7.0

# Required
numpy>=1.24.0
pyyaml>=6.0
pydantic>=2.5.0
fastapi>=0.104.0
