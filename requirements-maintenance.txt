# S3M Phase 17 — Procurement & Maintenance Dependencies

# Predictive models (optional — rule-based fallback available)
# scikit-learn>=1.3.0  # For GBM/RF models
# torch>=2.1.0  # For LSTM models (already in base if Phase 1 installed)

# Required (already in base requirements)
numpy>=1.24.0
pyyaml>=6.0
pydantic>=2.5.0
fastapi>=0.104.0
