# S3M GPU Training Dependencies — RunPod RTX 4090
# Install with: pip install -r requirements-gpu-training.txt

# Core ML
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.27.0
peft>=0.10.0
trl>=0.8.0
bitsandbytes>=0.43.0

# Unsloth (optional fast LoRA)
# Install separately: pip install unsloth
# unsloth[colab-new]  # for Colab/RunPod

# Merge & export
mergekit>=0.0.4
llama-cpp-python>=0.2.0

# Monitoring
wandb>=0.16.0
mlflow>=2.14.0

# Data
sentencepiece>=0.2.0
tiktoken>=0.6.0
protobuf>=4.25.0

# DeepSpeed (for Grok multi-GPU)
deepspeed>=0.14.0

# Evaluation
lm-eval>=0.4.0

# Existing S3M deps
pyyaml>=6.0
pydantic>=2.5.0
numpy>=1.24.0
psutil>=5.9.0
