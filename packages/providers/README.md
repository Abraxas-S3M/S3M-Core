# S3M Providers

This package contains provider-specific adapters built on top of
`packages/integration-sdk`.

- `_mock_provider` is the canonical reference implementation and test fixture.
- Live providers should follow the same dual-mode pattern.
