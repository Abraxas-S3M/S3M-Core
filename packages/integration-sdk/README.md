# S3M Integration SDK

Reusable provider integration framework for S3M dual-mode operations:

- **Online mode** for controlled development ingestion.
- **Air-gapped mode** for field deployment with no outbound calls.

This package provides base adapter contracts, authentication strategies,
resilient HTTP primitives, and provider registry infrastructure used by all
external provider adapters.
